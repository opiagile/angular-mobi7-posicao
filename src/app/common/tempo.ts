export class Tempo {
    placa: string;
    pontoInteresse: string;
    tempo: number;
  
    constructor(placa: string, pontoInteresse: string, tempo: number) {
      this.placa = placa;
      this.pontoInteresse = pontoInteresse;
      this.tempo = tempo;
    }
  }